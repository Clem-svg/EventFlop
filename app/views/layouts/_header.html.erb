 <!-- NAVBAR -->

    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
      <%= link_to "EventFlop", root_path, :class => "navbar-brand" %>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item active">
            <%= link_to "Créer un événement", new_event_path, :class => "nav-item nav-link" %>
          </li>
        </ul>

        <div class="dropdown">
          <% if current_user == nil %>
            <button type="button" class="btn btn-light">
              <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">S'inscrire / Se connecter</a>
              <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                <a class="dropdown-item" <%= link_to "Se connecter", new_user_session_path, :class => "nav-item nav-link" %></a>
                <a class="dropdown-item" <%= link_to "S'inscrire", new_user_registration_path, :class => "nav-item nav-link" %></a>
              </div>
            </button>
          <% else %>
            <form type="button" class="btn btn-light">
              <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Mon profil</a>
              <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                <a class="dropdown-item" <%= link_to "Mon profil", user_path(current_user.id), :class => "nav-item nav-link" %></a>
                <a class="dropdown-item" <%= link_to "Se déconnecter", destroy_user_session_path, :class => "btn btn-outline-danger", :method => 'DELETE' %></a>
              </div>
            </form>
          <% end %>
        </div>
      </div>
    </nav>
